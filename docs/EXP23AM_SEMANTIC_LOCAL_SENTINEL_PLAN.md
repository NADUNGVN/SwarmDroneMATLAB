# EXP23AM semantic local-route sentinel confirmation plan

Frozen: 2026-09-06, before execution.

EXP23AL `2026-09-06_071620` remains **invalid at 9/10 gates**. It passed all
210 structural routes, all 36,000 new conditional-relay traces, exact integer
byte/airtime accounting, route/certificate identity and retained-parent gates.
Its sole failing conjunct was the representation of one RESPONSE generated by
the initiator for itself:

- the semantic recipient set correctly becomes empty after removing the sender;
- no route is built and attempts, bytes and airtime are exactly zero;
- EXP23AJ correctly stored the undefined route hash as `NaN`;
- EXP23AL's confirmation code incorrectly required that undefined hash to equal
  numeric zero.

No scientific threshold, routing rule or outcome changes. EXP23AM freezes
`NaN` as the sentinel for an undefined physical route and reconstructs all 25
semantic sender-kind rows from the retained online migration fixture. Seven
gates require: the exact invalid-parent signature; retention of the other nine
EXP23AL gates; exactly one local-only row with the `NaN` sentinel and zero radio
cost; exact semantic recipient hashes; exact plans and reliability for the 24
nonlocal routes; exact v2 accounting/delivery; and no promotion of closed-loop,
renewal, robustness, fresh-seed or submission claims.

Passing EXP23AM closes only the routed-management primitive diagnostic by
combining EXP23AJ's 15 routing/scientific gates, EXP23AL's complete accounting
evidence, and this deterministic sentinel correction.

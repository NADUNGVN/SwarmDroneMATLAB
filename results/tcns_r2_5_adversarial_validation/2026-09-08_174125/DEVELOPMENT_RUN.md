# R2.5 development run

This is a complete development audit, not the authoritative R2.5 artifact.
It was generated while the new audit implementation was uncommitted.  Its
results were used to harden the summary and tests before the clean-commit
reproduction run.

The scientific registry and criteria are unchanged.  The authoritative run is
the later directory referenced by this experiment family's `LATEST.txt`.
